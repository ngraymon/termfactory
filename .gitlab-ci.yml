stages:
    - code_analysis

default:
  image: python:3.8

cov_test:
  stage: code_analysis
  tags: [ci]
  script:
    - py -m pip install -r requirements.txt
    - py -m pip install -r test_requirements.txt
    - py vulture_wrapper.py
    # - py -m vulture --min-confidence 100 generate_residual_equations.py
    # - vulture generate_residual_equations.py --min-confidence 100
    #- py -m coverage run generate_residual_equations.py
    #- py -m coverage html
  # artifacts:
  #   paths:
  #       - .\htmlcov\*.css
  #       - .\htmlcov\*.js
  #       - .\htmlcov\generate_residual_equations_py.html

    # reports:
    #   cobertura:
    #     - .\htmlcov\*.css
    #     - .\htmlcov\*.js
    #     - .\htmlcov\generate_residual_equations_py.html, .\htmlcov\*.css
