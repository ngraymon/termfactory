stages:
    - code_analysis

default:
  image: python:3.9
  tags: [lightsail]

# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

  # prevent gitlab from running git clean on every job and destroying the cache
  GIT_CLEAN_FLAGS: "-ffdx -e .cache/,venv/"
  GIT_STRATEGY: "none"
  GIT_CHECKOUT: "false"

# Pip's cache doesn't store the python packages
# https://pip.pypa.io/en/stable/reference/pip_install/#caching
#
# If you want to also cache the installed packages, you have to install
# them in a virtualenv and cache it as well.
cache:
  paths:
    - .cache/pip
    - venv/

before_script:
  - python -V               # Print out python version for debugging
  - pip install virtualenv
  - virtualenv venv
  - source venv/bin/activate
  - python -m pip install -r requirements.txt
  - python -m pip install -r test_requirements.txt

cov_test:
  stage: code_analysis
  script:
    - python vulture_wrapper.py
    - python -m coverage run generate_residual_equations.py logging_output.txt
    - python -m coverage html
    - python -m coverage report

  artifacts:
    paths:
        - ./logging_output.txt
        - ./vulture_output.txt
        - ./htmlcov/*.css
        - ./htmlcov/*.js
        - ./htmlcov/generate_residual_equations_py.html

    # reports:
    #   cobertura:
    #     - ./htmlcov/*.css
    #     - ./htmlcov/*.js
    #     - ./htmlcov/generate_residual_equations_py.html, ./htmlcov/*.css
